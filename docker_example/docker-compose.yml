version: "3.8"

services:
  langflow:
    image: langflowai/langflow:latest
    networks:
      - dokploy-network
    ports:
      - "7860"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.langflow.entrypoints=websecure"
      - "traefik.http.routers.langflow.tls.certResolver=letsencrypt"
      - "traefik.http.routers.langflow.rule=Host(`langflow.k2solucoessolar.com.br`)"
      - "traefik.http.services.langflow.loadbalancer.server.port=7860"
    environment:
      - LANGFLOW_DATABASE_URL=postgresql://aleiola:ADEJ@30315@k2-solues-solar-postgres-4de737:5432/langflow
    volumes:
      - langflow-data:/app/langflow

volumes:
  langflow-data:

networks:
  dokploy-network:
    external: true

